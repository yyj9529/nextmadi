# 조용한 실패가 성공처럼 보임

이 계열이 가장 위험하다. 실패가 실패처럼 보이지 않고 **성공처럼 보인다.**

## 증상

- eval 전 케이스가 정확히 1.00점 → API 크레딧 부족으로 호출이 실패했는데 예외 핸들러가
  최저점 1을 채워 넣고 있었다. "낮은 점수"로 보였지 "호출 안 됨"으로 보이지 않았다
  (`20260610_1104`)
- 리팩터링 스크립트의 import 주입 단계가 **조용히 실패**해 97개 컴파일 에러로 뒤늦게
  드러남 (`20260619_1315`)
- `GlobalExceptionHandler`가 미처리 예외를 **로깅 없이** 삼킴. 500은 떴지만 원인이
  어디에도 남지 않았다 (`20260704_0525`)
- 회귀 방어 테스트가 **설정 객체의 모양**만 봤다. 모양은 그대로인데 런타임 경로가 깨져
  Google/Kakao 로그인이 전부 죽어 있었고 스위트는 초록불이었다. 게다가 다른 테스트는 그
  깨진 동작(어댑터가 던지는 것)을 정답으로 고정해두고 있었다 (`20260828`)
- 새로 쓴 테스트에서 `await expect(...).rejects`의 `await`를 빼먹었다. reject 여부를 실제로
  검사하지 않는데 초록불이 뜬다 — "테스트 없음"이 "가짜 테스트 있음"으로 바뀔 뻔했다
  (`20260828`)
- 커밋 전 게이트를 `set -e` 체인 안에 `(gradlew spotlessCheck) && echo pass`로 넣었다.
  spotlessCheck가 실패했는데 커밋, push, PR 생성까지 그대로 진행됐다. bash의 `set -e`는
  `&&` 목록에서 마지막이 아닌 명령이 실패해도 멈추지 않는다 (`20260910_0903`)

## 근본 원인

**예외 핸들러가 실패를 유효한 값으로 변환한다.** `except: scores = 1` 같은 fallback은
실패 신호를 정상 데이터 형태로 바꿔버린다. 호출부에서는 "실패"와 "낮은 값"이 구별되지
않는다.

테스트 쪽에도 같은 구조가 있다. 아무것도 검사하지 않는 단언(`await` 빠진 `rejects`,
런타임이 아니라 설정의 모양만 보는 어서션)은 **없는 것보다 나쁘다** — 커버리지가
있다는 착각을 만든다.

## 올바른 방법

- 실패 경로는 값을 만들어내지 말고 **구별 가능한 표식**을 남긴다 (null, error 필드,
  명시적 예외). 점수 자리에 최저점을 채우지 않는다
- 예외를 삼켜야 한다면 **삼킨 이유를 로그로 남긴다**. 조용한 catch는 관측성 결함이다
- **새 테스트는 대상 코드를 일부러 깨뜨려 빨간불을 확인한 뒤 커밋한다.** 초록불은 테스트가
  무엇을 검사하는지 말해주지 않는다. 비동기 단언은 특히 — `await`이 빠지면 아무것도 검사하지
  않으면서 통과한다
- **회귀 방어는 설정이 아니라 실행 경로를 검사한다.** 설정 객체의 모양이 같아도 런타임은
  깨질 수 있다 ([library-call-sites-unread](library-call-sites-unread.md))
- fail-fast의 대가를 인식한다: 포맷 검사가 먼저 실패하면 그 뒤 컴파일 에러 15개가
  가려진다. 표면 메시지를 통과시킨 뒤 전체를 다시 돌린다
- **셸에서 검사 결과로 흐름을 멈춰야 하면 종료 코드를 명시적으로 확인한다.**
  `if ! cmd; then exit 1; fi`로 쓴다. `set -e`는 `&&`/`||` 목록과 `if` 조건 안의 실패를
  무시하고, 파이프는 `pipefail` 없이는 마지막 명령의 상태만 본다. 이 노트의 `tee` 두 건과
  `&&` 한 건이 모두 이 규칙에서 나왔다

## 자동화 후보

재발 7회. 4회 이상이므로 "관행" 수준의 조치는 이미 실패한 것으로 본다.

- eval 스크립트의 예외 fallback 제거 또는 `status: error` 필드 분리 — `eval/s07-analysis/`
- `await` 없는 `expect(...).rejects` / `.resolves`를 lint로 차단
  (`@typescript-eslint/no-floating-promises` 또는 전용 규칙)

## 재발 이력

- `20260916_s07-quality-recovery` (실행 오류를 품질 점수와 구분하고 실패 비용 보존; API 검증 대기.)

- `20260610_1104` (eval fallback이 최저점을 채움)
- `20260619_1315` (import 주입 단계 조용히 실패)
- `20260704_0525_local-e2e-verify-analysis-500` (예외를 로깅 없이 삼킴)
- `20260828_ticket-19-u6-u7-review` (설정 모양만 보는 회귀 테스트 + `await` 빠진 `rejects`)
- `20260906_eval-case-criteria-review` (eval CI가 파이프 뒤 `tee` 종료코드만 보고 초록불. API 키 시크릿이 없어 실행기가 즉시 죽었는데 5회 연속 success로 기록됐다)
- `20260909_1111_s07-eval-judge-v3-baseline` (로컬 eval 실행을 `| tee`로 파이프해 크래시가
  exit 0으로 보고됐다. 바로 위 `20260906` 항목이 CI에서 고친 것과 같은 원인을 3일 뒤 로컬에서
  반복한 것이다)
- `20260910_0903_public-release-prep` (`set -e` 체인의 `&&` 안에 넣은 spotlessCheck가 실패했는데
  커밋·push·PR 생성이 진행됐다. 위반은 줄바꿈 artifact라 무해했지만 게이트는 작동하지 않았다)
- 그 외 3건은 2026-08-25 소급 집계분으로, 개별 dev-log에 귀속하지 못했다.
  당시 집계는 7회였고 위 4건이 본문에서 확인된 것이다

- `20260919_0500_s07-alignment.md` (뒤 명령의 성공이 앞 lint 실패를 가리지 않도록 개별 확인; 평가자 판단 불가와 실행 오류를 별도로 검사.)
- `20260923_0920_175-refuse-dev-internal-auth-secret` (거부 테스트 2건이 `hasFailed()`만 봐서
  다른 이유의 기동 실패도 통과시킬 수 있었다. 빨간불 확인은 통과했다 — 검사를 빼면 기동이
  성공하므로. spec-reviewer가 잡음)

## Related

- [testcontainers-skipped-locally](testcontainers-skipped-locally.md) — 같은
  "초록불이 아무것도 증명하지 않는다" 계열이지만 원인이 다르다. 저쪽은 환경 때문에
  테스트가 안 돈 것이고, 이쪽은 돌았는데 실패를 삼킨 것이다.
- [ambient-env-in-tests](ambient-env-in-tests.md) — 테스트가 환경 때문에 조용히
  다른 경로를 타는 경우.
